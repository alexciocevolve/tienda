POSTGRES_PASSWORD=shop
DATABASE_URL=postgresql+psycopg://shop:shop@localhost:5432/shop
# Pages allowed to call the API from a browser (comma-separated origins, no path).
# To deploy the frontend elsewhere, list its address here, e.g. https://shop.example.com
CORS_ORIGINS=http://localhost:5173
