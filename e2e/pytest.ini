[pytest]
# Kept apart from backend/tests on purpose. Those run in-process against a throwaway
# database and need nothing switched on; these need the whole shop up, and talk to it the
# way a customer's browser would.
testpaths = .
addopts = -q --tb=short
